责任链模式
意图： 让一个请求有可能被多个对象处理，每个对象都有可能处理该请求，这个时候将这些对象连成一条链，然后将请求沿着这条链进行传递直到有一个对象处理它为止。
好处：将请求的发送者和请求的接受者进行解耦。
后记：这有点像一种责任推卸模式，如果我没法处理这种职责，我就往后面推，直到承担该职责的模块进行处理，这在现实生活中的应用场景是比较少。更多的是像Servelt中的过滤器，
           每个过滤器都会对请求进行处理，然后把处理后的请求传递给前一个过滤器。